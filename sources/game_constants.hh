#pragma once

#include <SDL2/SDL_stdinc.h>

constexpr uint32_t MAX_ROBOT_GUI_LINES                = 400;
constexpr uint32_t IMGUI_VERTEX_BUFFER_CAPACITY_BYTES = 200 * 1024;
constexpr uint32_t IMGUI_INDEX_BUFFER_CAPACITY_BYTES  = 160 * 1024;
